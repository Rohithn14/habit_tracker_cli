## What does this PR do?

<!-- One-line summary -->

## Why?

<!-- Motivation, issue reference, or context -->

## Test plan

- [ ] `uv run pytest` passes
- [ ] Tested the changed command(s) manually
- [ ] `habit summary` still exits 0 silently with no DB (if shell path touched)
